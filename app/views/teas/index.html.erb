
<div class="row-flex row-flex-wrap">
  <% @teas.each do |t| %>

      <div class="jumbotron col-md-4" >
        <div class="col-sm-12" style="word-wrap: break-word; ">
          <div class="col-sm-12">
            <h2><%=  t.name %></h2>
            <%= tea.country %>
            <%= tea.kind %>
          </div>
          <%= link_to 'Show', tea_path(tea)  %>
          <% if user_signed_in? %>|
              <%= link_to 'Edit', edit_tea_path(tea)  %> |  <%= link_to 'Delete', tea_path(tea), method: :delete  %>
          <% end %>
          </td>
          </tr>
  <% end %>
          <div class="clearfix"></div>

        </div>
      </div>
  <% end %>
</div>
<div class="row">
  <%= will_paginate @teas %>
</div>